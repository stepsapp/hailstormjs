const React = require('react');
import Document from '../components/document';

export default () => (
    <Document>
        <h1>Welcome</h1>
        <h2>This is your entry point</h2>
    </Document>
)